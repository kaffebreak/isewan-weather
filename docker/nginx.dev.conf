server {
    listen 80;
    server_name localhost;

    # セキュリティヘッダー
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # ルート: 気象データ(/weather)と船舶管制2システムへのリンクを並べた
    # 簡単なメニューページ
    location = / {
        root /usr/share/nginx/html;
        index menu.html;
    }

    # 末尾スラッシュなしでアクセスされた場合は正規化する
    location = /weather {
        return 301 /weather/;
    }

    # 気象データ（開発サーバー）。Viteの開発サーバー自体は base=/ のまま
    # なので、ここでプレフィックスを外してから転送する。
    location /weather/ {
        proxy_pass http://localhost:5173/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket対応（Vite HMR用）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 名古屋港 船舶管制（別リポジトリ: kaffebreak/nagoya-control、isewan-edge ネットワーク経由）
    location = /nagoya-control {
        return 301 /nagoya-control/;
    }

    location /nagoya-control/ {
        proxy_pass http://nagoya-control:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 伊良湖水道 船舶管制（別リポジトリ: kaffebreak/irago-schedule、公開パスは /irago-control）
    # 内部ポートは他システムと合わせて8000に統一（80/443はこのnginxの専用領域）。
    location = /irago-control {
        return 301 /irago-control/;
    }

    location /irago-control/ {
        proxy_pass http://irago-control:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # バックエンドAPI
    location /api/ {
        proxy_pass http://isewan-weather-dev:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # APIタイムアウト設定
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # ヘルスチェック用エンドポイント
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
